# existing code...
def is_capability_question(question: str) -> bool:
    """
    Checks if a question is related to a capability.
    
    Args:
        question (str): The question to check.
    
    Returns:
        bool: True if the question is related to a capability, False otherwise.
    """
    # existing code...
def capability_response(capability: str) -> str:
    """
    Generates a response for a capability.
    
    Args:
        capability (str): The capability to generate a response for.
    
    Returns:
        str: The response for the capability.
    """
    # existing code...